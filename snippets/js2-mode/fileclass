# -*- mode: snippet; require-final-newline: nil -*-
# name: fileclass
# key: C
# --
import EventEmitter from 'events';

export default class `(file-name-sans-extension (file-name-nondirectory (buffer-file-name)))` extends EventEmitter {
    constructor (opts = {}) {
        super();
        $0
    }
};